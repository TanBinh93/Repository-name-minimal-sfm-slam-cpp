# Tests

This folder is reserved for future unit tests and integration checks.

A later version of the project can add GoogleTest or Catch2 tests for geometry, pose estimation, and point cloud utilities.
